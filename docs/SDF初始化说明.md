# SDF 初始化说明

> **文档维护**：本文档是 SDF 初始化的唯一权威说明。实现代码必须与本文档保持一致；
> **实现完成后需要保存并更新本文档**，后续任何对 SDF 初始化的修改都要同步修订本文档。

---

## 1. 目标

把 SDF 网络 `phi(x, y, t)` 在 **t = 0 切片**上预训练到初始（全材料）符号距离函数，为后续力学初始化和 HJB 演化提供正确的材料几何。

## 2. SDF 的物理含义

- φ > 0：有材料；
- φ < 0：空洞；
- φ = 0：材料/空洞界面。

材料指示函数由光滑 Heaviside 给出：

$$
S(\phi) = \frac{1}{2}\left(1 + \tanh\frac{\phi}{2\beta}\right)
$$

β 是过渡带宽度参数（过渡带宽约 4β）。

## 3. 初始形状（全材料）

初始设计为整块实体，SDF 取到设计域四边的最小距离（域内为正）：

$$
\phi_0(x, y) = \min\big(x,\; L_x - x,\; y,\; L_y - y\big)
$$

这是真正的距离函数（|∇φ| = 1，中轴线处不可导）。目标是让网络拟合这个解析场，因此 SDF 初始化是一个**有监督回归**。

> **offset 说明**：上式零水平集正好压在域边界上，因此边界上 φ=0、S=0.5（半材料）。
> 若希望边界也是满材料，可给 φ₀ 加一个整体偏移 offset ≥ 5·(2β)，把零水平集推到域外。
> 是否加 offset 是一个需要记录的设计决策。

## 4. 损失函数

关键观察：**S = H(φ) 只对 |φ| ≲ 过渡带的窄带敏感**，而这条带（零水平集附近）正是拟合误差最要命的地方。均匀采样下带内点占比只有 ~带宽/面积，必须显式加采样、加权。

因此损失分两项：

$$
\mathcal{L} = \underbrace{\frac{1}{N_i}\sum_{\text{内部}} \big(\phi - \phi_0\big)^2}_{\text{内部 MSE}}
\;+\; w_{bnd}\cdot \underbrace{\frac{1}{N_b}\sum_{\text{边界带}} \big(\phi - \phi_0\big)^2}_{\text{边界带 MSE}}
$$

- 内部点：域内均匀 / Sobol 采样；
- 边界带点：沿四边内侧窄带采样（按边长比例分配，隔点严格压在边上，外加 4 个角点）；
- 权重 w_bnd ≈ 10。

## 5. 训练流程（两阶段）

**阶段 A：Adam（每步重采样）**
- 每步重新采样内部点 + 边界带点（起到正则化作用，见过的点远多于固定数据集）；
- 学习率 ~2e-3，数千步。

**阶段 B：L-BFGS 精调（固定点集）**
- 在固定的大点集（Sobol 内部点 + 边界带点）上全批量训练，强 Wolfe 线搜索；
- 分块执行（每块若干次内部迭代），用**独立验证集**只评估不训练；
- 连续若干块相对改善低于阈值则早停；
- 结束后**回滚到验证损失最优的权重**（防止过拟合固定配点）。

## 6. 关键注意事项（踩过的坑）

1. **张量形状必须逐点对应**。目标 φ₀ 对 (N,1) 输入返回 (N,1)，**不要再额外 unsqueeze**。
   否则预测 (N,1) 与目标 (N,1,1) 会被广播成 (N,N,1)，损失变成所有点对之差的均值，
   最优解退化成输出常数 mean(φ₀)，损失卡在 Var(φ₀) 降不下来。**这是之前损失卡死的根因。**

2. **边界带必须加采样加权**。不加的话，全域 MSE 看着不错，但边界带（决定 S 的地方）误差很大，
   导致材料场在边界附近失真。

3. **float64**。L-BFGS 线搜索在 float32 下容易提前停滞。

## 7. 诊断（训练后自检）

- 拟合误差：全域 max、边界带内 max（带内误差应明显小于过渡带宽度 2β）；
- |∇φ| 统计：距离函数应处处 ≈ 1（中轴线折点附近被网络抹平、偏小属正常）；
- 边界上的 S 值（offset=0 时应为 0.5）。

## 8. 输出 / 保存

- 训练完成后保存 SDF 网络权重（如 `phi_init.pt`），供力学初始化和后续演化加载。

---

*下一步：见《应力位移初始化说明》。*
